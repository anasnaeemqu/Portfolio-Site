## Packages
(none needed)

## Notes
framer-motion is already installed and used for page/section animations
All data is fetched from backend via @shared/routes endpoints (no hardcoded portfolio content in UI)
SEO is handled client-side by setting document.title + meta[name="description"]
